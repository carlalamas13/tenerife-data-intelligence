services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: tenerife
      POSTGRES_USER: tenerife
      POSTGRES_PASSWORD: tenerife
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
